# Proc ignores another ones arguments e consider only the number of args especified
    # |first, last| / ("one", "two", "three") ---> #"one, two"
# Lambda throw an error when more than the especified arguments are passed.
    # |first, last| / ("one", "two", "three") ---> #ERROR